הדבק ב־Claude Code מתוך תיקיית האתר, אחרי שהעתקת את התיקייה game61/ לשורש הריפו:

---
שלב את המשחק "61 — יש לך דקה" שבתיקייה game61/ באתר, לפי game61/README-he.md.

1. קרא קודם את package.json, מבנה הפרויקט, עמוד הבית ואופן הפריסה, וכתוב לי סיכום קצר. אל תניח framework או אחסון.
2. הוסף עמוד /game (נתיב שמתאים לקוד הקיים) שמגיש את המשחק, וכפתור בולט בעמוד הבית: "חדש: המשחק 61 — יש לך דקה". אל תשנה את מונה התמיכות או תוכן קיים.
3. ודא ש־public/game/polls.json מוגש בכתובת /game/polls.json.
4. עדכון אוטומטי:
   - בדוק את https://themadad.com/robots.txt ואת דף ההנחיות לסוכנים באתר. אם הם אוסרים שליפה אוטומטית — עצור ודווח לי.
   - הרץ `node game61/poll-updater/update-polls.mjs --out /tmp/polls.json` מול האתר החי. אם הפענוח נכשל, התאם את parseAverageTable למבנה האמיתי, שמור את ה־HTML האמיתי כ־fixture נוסף והוסף לו בדיקה.
   - העבר את .github/workflows/update-polls.yml לשורש והתאם נתיבים. אם האתר לא נפרס אוטומטית מ־commit, הצע חלופה (cron של ספק האחסון) שמשתמשת באותו lib/core.mjs.
5. עדכן את CLAUDE.md של הפרויקט: מצב המשחק הראשי מבוסס כעת על ממוצע סקרים מפורסם עם ציון מקור, תאריך ותקופת איסור; המצב הדמיוני נשאר משני. השאר את שאר כללי הפרטיות והנגישות.
6. הרץ את כל הבדיקות (game61/game/engine.test.cjs, game61/poll-updater/test/core.test.mjs, וסקריפטי build/lint קיימים) ודווח תוצאות אמיתיות.
7. אל תפרוס לייצור ואל תפעיל את ה־workflow בלי אישור מפורש ממני.
---
